<ui:UiPage x:Class="BeastStrap.UI.Elements.Settings.Pages.ObfuscatorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
      xmlns:controls="clr-namespace:BeastStrap.UI.Elements.Controls"
      xmlns:models="clr-namespace:BeastStrap.UI.ViewModels.Settings"
      d:DataContext="{d:DesignInstance Type=models:ObfuscatorViewModel}"
      mc:Ignorable="d"
      d:DesignHeight="560" d:DesignWidth="820"
      Title="ObfuscatorPage"
      Scrollable="False">

    <ui:UiPage.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style x:Key="StatusText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsError}" Value="True">
                    <Setter Property="Foreground" Value="#F0A0A0" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </ui:UiPage.Resources>

    <Grid Margin="0,0,14,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Intro -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock FontSize="14" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       Text="Obfuscate a Lua / Luau script. Pick an engine, paste your script, and hit Obfuscate." />
            <TextBlock FontSize="11.5" Margin="0,4,0,0" Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                       Text="Heads up: your script is uploaded to the third-party obfuscation API to be processed." />
        </StackPanel>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Engine" VerticalAlignment="Center" Margin="0,0,8,0"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <ComboBox Grid.Column="1" MinWidth="175" VerticalAlignment="Center"
                      ItemsSource="{Binding Engines, Mode=OneTime}" SelectedItem="{Binding SelectedEngine, Mode=TwoWay}" />
            <TextBlock Grid.Column="2" Text="Target" VerticalAlignment="Center" Margin="16,0,8,0"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" Visibility="{Binding LeakdOptionsVisibility}" />
            <ComboBox Grid.Column="3" MinWidth="150" VerticalAlignment="Center" Visibility="{Binding LeakdOptionsVisibility}"
                      ItemsSource="{Binding Presets, Mode=OneTime}" SelectedItem="{Binding SelectedPreset, Mode=TwoWay}" />
            <ui:Button Grid.Column="5" Icon="Sparkle24" Content="Beautify" Margin="0,0,8,0"
                       Command="{Binding BeautifyCommand}" IsEnabled="{Binding NotBusy}" />
            <ui:Button Grid.Column="6" Icon="LockClosed24" Content="Obfuscate" Appearance="Primary"
                       Command="{Binding ObfuscateCommand}" IsEnabled="{Binding NotBusy}" />
        </Grid>

        <!-- LuaObfuscator API key (shown only for that engine) -->
        <Grid Grid.Row="2" Margin="0,0,0,8" Visibility="{Binding LuaObfuscatorOptionsVisibility}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="API key" VerticalAlignment="Center" Margin="0,0,8,0"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <ui:TextBox Grid.Column="1" Icon="Key24" PlaceholderText="Your luaobfuscator.com API key"
                        Text="{Binding ApiKey, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Grid.Column="2" Text="saved to your local settings only" VerticalAlignment="Center" Margin="10,0,0,0"
                       FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
        </Grid>

        <!-- Editors -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Input -->
            <Grid Grid.Column="0" Margin="0,0,6,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Script" FontWeight="SemiBold" VerticalAlignment="Center" />
                    <ui:Button Grid.Column="1" Icon="ClipboardPaste24" Content="Paste" Padding="8,3" FontSize="12" Command="{Binding PasteCommand}" />
                    <ui:Button Grid.Column="2" Icon="EraserSmall24" Content="Clear" Padding="8,3" FontSize="12" Margin="6,0,0,0" Command="{Binding ClearCommand}" />
                </Grid>
                <avalonedit:TextEditor Grid.Row="1" Style="{StaticResource NewTextEditor}"
                                       ShowLineNumbers="True" WordWrap="False"
                                       FontFamily="Consolas" FontSize="12.5" MinHeight="240"
                                       controls:CodeEditor.BoundText="{Binding ScriptInput, Mode=TwoWay}" />
            </Grid>

            <!-- Output -->
            <Grid Grid.Column="1" Margin="6,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Result" FontWeight="SemiBold" VerticalAlignment="Center" />
                    <ui:ProgressRing Grid.Column="1" Width="16" Height="16" IsIndeterminate="True" VerticalAlignment="Center" Margin="0,0,6,0"
                                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />
                    <ui:Button Grid.Column="2" Icon="Copy24" Content="Copy" Padding="8,3" FontSize="12"
                               Command="{Binding CopyOutputCommand}" IsEnabled="{Binding HasOutput}" />
                </Grid>
                <avalonedit:TextEditor Grid.Row="1" Style="{StaticResource NewTextEditor}"
                                       ShowLineNumbers="True" WordWrap="False" IsReadOnly="True"
                                       FontFamily="Consolas" FontSize="12.5" MinHeight="240"
                                       controls:CodeEditor.BoundText="{Binding Output, Mode=OneWay}" />
            </Grid>
        </Grid>

        <!-- Status -->
        <TextBlock Grid.Row="4" Margin="0,8,0,0" Style="{StaticResource StatusText}" Text="{Binding StatusText}" />
    </Grid>
</ui:UiPage>
