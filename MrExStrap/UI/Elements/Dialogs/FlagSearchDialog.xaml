<base:WpfUiWindow x:Class="BeastStrap.UI.Elements.Dialogs.FlagSearchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:base="clr-namespace:BeastStrap.UI.Elements.Base"
        mc:Ignorable="d"
        Title="Browse fast flags"
        Width="540" Height="600" MinWidth="420" MinHeight="360"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Padding="8" Title="Browse fast flags" ShowMinimize="False" ShowMaximize="False"
                     CanMaximize="False" Icon="pack://application:,,,/BeastStrap.ico" />

        <TextBlock Grid.Row="1" Margin="16,4,16,8" TextWrapping="Wrap" FontSize="13"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Text="Search Roblox's live list of known fast flags. Pick one and Add to drop it straight into the editor. This confirms a flag actually exists, so you don't waste time on typos or dead flags." />

        <ui:TextBox Grid.Row="2" Margin="16,0,16,8" x:Name="SearchBox" Icon="Search32"
                    PlaceholderText="Search flag names…" TextChanged="SearchBox_TextChanged" />

        <Border Grid.Row="3" Margin="16,0,16,0" CornerRadius="6" BorderThickness="1"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                Background="{DynamicResource ControlFillColorDefaultBrush}">
            <ListBox x:Name="FlagList" Background="Transparent" BorderThickness="0"
                     FontFamily="Consolas" MouseDoubleClick="FlagList_MouseDoubleClick" />
        </Border>

        <Grid Grid.Row="4" Margin="16,10,16,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="CountText" VerticalAlignment="Center" FontSize="12"
                       Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            <ui:Button Grid.Column="1" Appearance="Primary" Icon="Add28" Content="Add to editor"
                       Click="AddButton_Click" />
            <ui:Button Grid.Column="2" Margin="8,0,0,0" Content="Close" Click="CloseButton_Click" IsCancel="True" />
        </Grid>
    </Grid>
</base:WpfUiWindow>
